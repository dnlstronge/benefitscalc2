import React from "react";
import classes from "./LCW.module.css"
import UCElements from "../../UCElements/UCElements"

const UC_elements = UCElements

const LCW = (props) => {


    LCW_handler = (e) => {
        set
    }


    return (
        <div className={LCW_container}>
            <label htmlFor="selectLCW">If this is an ESA migration case with LCW, add LCW component to
            claim <select id="selectLCW">
                <option>--select--</option>
                <option>Yes</option>
                <option>No</option>
            </select>
            </label>
            
            
        </div>
    )
}

export default LCW;